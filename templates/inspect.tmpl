<!doctype html>
<html>
{{template "head"}}
    <body>
	{{template "navbar" .}}
	<div class="container-fluid">
		<h2 class="text-center" id="message"></h2>
		<div class="col-xs-3"></div>
		<div class="col-xs-6">
		</div>
		<div class="col-xs-3"></div>
	</div>
    </body>
    <script>
	mainUrl={{.Url}};
	houses={{.Data}};
	contract=houses[0].contract;

	$("#message").text("Inspection for " + contract);
    </script>
</html>
